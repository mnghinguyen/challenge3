
// Function Ask user for password length
let generatePassword = prompt("Enter your password length (or number of characters) 8, 12 or 16", "8");
if (generatePassword != null) {
  document.getElementById("password").innerHTML =
 "hello";
}